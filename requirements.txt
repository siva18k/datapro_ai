# Core runtime dependencies for the API, MCP server, ingestion, and DB access.
# Optional/analysis-heavy packages were removed to keep the Docker image smaller.
pg8000
requests
certifi
python-dotenv
pypdf
mcp
fastapi
uvicorn
python-multipart
