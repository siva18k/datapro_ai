.DS_Store
__pycache__/
*.pyc

# Cursor IDE (local agent rules — not part of the project)
.cursor/

# Local TLS / proxy certificates
certs/

# Secrets and local config — use .env.example and saved_db_connections.json.example
.env
.env.*
!.env.example
saved_db_connections.json
!saved_db_connections.json.example
secrets.toml
.streamlit/secrets.toml

# Runtime / local artifacts
outputs/
chroma_store/
.mcp_server.pid
.mcp_server.log
.email_mcp.pid
.email_mcp.log
.api_server.pid
.api_server.log

# Local Python environments and package caches
.venv/
.venv*/
venv/
venv*/
env/
env*/
**/.venv/
**/venv/
**/__pycache__/
**/*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.pip-wheel-metadata/
*.egg-info/
.eggs/
build/
dist/

# Frontend build and local installs
web/node_modules/
web/dist/
*.tsbuildinfo
node_modules/
.npm/
.pnpm-store/

# Local editor / tool caches
.vscode/
.idea/

# TLS / proxy certs (keep under certs/ locally — never commit)
*.cer
*.crt
!sample_docs/**

# Legacy Streamlit UI (removed)
pages/

# Personal / production AWS deployment (ECS Fargate, account IDs, ARNs, secrets)
# Templates live in deploy.example/ — copy locally with: ./deploy.example/scripts/init-deploy.sh
deploy/
